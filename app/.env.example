# Copia este archivo a .env y completa los valores
# cp .env.example .env

# Clave secreta para firmar los tokens JWT
# Genera una segura con: python -c "import secrets; print(secrets.token_hex(32))"
# Si se deja vacía, la app genera una clave aleatoria por proceso (ver
# app/core/security.py) — válido para desarrollo, pero en producción debe
# definirse explícitamente para que las sesiones sobrevivan a un reinicio.
SECRET_KEY=

# Entorno de ejecución: development | production
FLASK_ENV=development

# Depuración del servidor Flask cuando se ejecuta sin gunicorn
FLASK_DEBUG=false

# Puerto expuesto por la aplicación
PORT=5000

# Ruta del archivo SQLite al ejecutar localmente desde ./app
DATABASE_PATH=olimpiadas.db
